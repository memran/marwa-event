{
  "name": "memran/marwa-event",
  "description": "Lightweight PSR-14 event dispatcher for PHP 8.2+ with stable priority ordering, stoppable events, and optional container-aware resolution.",
  "type": "library",
  "license": "MIT",
  "keywords": [
    "events",
    "psr-14",
    "dispatcher",
    "listener"
  ],
  "require": {
    "php": ">=8.2",
    "psr/event-dispatcher": "^1.0",
    "psr/container": "^2.0"
  },
  "autoload": {
    "psr-4": {
      "Marwa\\Event\\": "src/"
    }
  },
  "config": {
    "allow-plugins": {
      "infection/extension-installer": false
    }
  },
  "require-dev": {
    "friendsofphp/php-cs-fixer": "^3.89",
    "infection/infection": "^0.31",
    "phpstan/phpstan": "^2.1",
    "phpunit/phpunit": "^10.0"
  },
  "scripts": {
    "test": "vendor/bin/phpunit",
    "test:coverage": "php scripts/run-coverage.php",
    "analyse": "vendor/bin/phpstan analyse --no-progress --debug",
    "mutate": "php scripts/run-mutation.php",
    "lint": "vendor/bin/php-cs-fixer fix --dry-run --diff --verbose",
    "fix": "vendor/bin/php-cs-fixer fix --verbose",
    "ci": [
      "@test",
      "@analyse",
      "@lint"
    ]
  }
}
